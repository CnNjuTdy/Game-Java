package model.equipment;

import model.ItemList;

public class EquipmentList extends ItemList {
    public String desc = "装备列表";

    public EquipmentList(int maxSize) {
        super(maxSize);
    }
}
